<%= form_for :comment, url: comments_path do |f| %>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>

  <%= hidden_field_tag :discussion_id, params[:discussion_id] %>

  <p>
    <%= f.submit %>
  </p>
<% end %>
